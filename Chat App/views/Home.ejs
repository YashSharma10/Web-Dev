<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web chit</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #202124; /* Dark background color */
            margin: 0;
            padding: 0;
            height: 100vh; /* Adjusted to full viewport height */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .logo-container,
        .chat-container {
            max-width: 400px;
            background-color: #2c2f33; /* Darker chat container background color */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            opacity: 0;
            transform: translateY(-50px);
            position: relative;
            animation: slideIn 2s ease forwards;
        }

        .logo-container img {
            width: 200px;
            height: auto;
            display: block;
            margin: 0 auto;
        }

        .chat-container {
            display: none;
            color: #fff; /* Text color for dark theme */
            text-align: center; /* Center-align the chat option */
            font-size: 24px; /* Increase font size for better visibility */
        }

        .chat-container.active {
            display: block;
        }

        .chat-container form {
            display: flex;
            justify-content: center;
        }

        .chat-container button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin: 0 5px; /* Add margin to create space between buttons */
        }

        .chat-container button:hover {
            background-color: #0056b3;
        }

        @keyframes slideIn {
            0% {
                opacity: 0;
                transform: translateY(-50px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="logo-container">
        <img src="https://i.ibb.co/Lh98B6D/Whats-App-Image-2024-04-22-at-22-08-48-36006a67.jpg" alt="Logo">
    </div>
    <div class="chat-container" id="chatContainer">
        <h1>Welcome to WebChit</h1>
        <form method="get" action="/chats/new">
            <button>New Chat</button>
        </form><br>
        <form method="get" action="/chats">
            <button>View Chat</button>
        </form>
    </div>

    <script>
        // Trigger chat display after logo animation
        document.addEventListener('DOMContentLoaded', function () {
            setTimeout(function () {
                document.querySelector('.logo-container').style.display = 'none';
                document.querySelector('.chat-container').classList.add('active');
            }, 2000); // Adjust the delay according to your animation duration
        });
    </script>
</body>
</html>
