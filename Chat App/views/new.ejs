<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #202124;
            /* Dark background color */
            margin: 0;
            padding: 0;
            height: 100vh;
            /* Adjusted to full viewport height */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .chat-container {
            max-width: 800px;
            background-color: #2c2f33;
            /* Darker chat container background color */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            opacity: 1;
            /* Removed animation properties */
            position: relative;
            color: #121010;
            /* Text color for dark theme */
            text-align: center;
            /* Center-align the chat option */
            font-size: 24px;
            /* Increase font size for better visibility */
        }

        .chat-container.active {
            display: block;
        }

        .chat-container h3 {
            margin: 0 0 20px;
            font-size: 24px;
            color: #eee;
            /* Header text color for dark theme */
        }

        .chat-form {
            display: flex;
            flex-direction: column;
        }

        .chat-form input,
        .chat-form textarea,
        .chat-form button,
        .all-chats-btn {
            margin-bottom: 10px;
            padding: 10px;
            border: 1px solid #444;
            /* Darker border color */
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s ease;
            background-color: #d5e3f3;
            /* Darker input background color */
            color: #ffeded;
            /* Text color for dark theme */
            cursor: pointer;
        }

        .chat-form input:focus,
        .chat-form textarea:focus,
        .chat-form button:focus,
        .all-chats-btn:focus {
            border-color: #0056b3;
            outline: none;
        }

        .chat-form button:hover,
        .all-chats-btn:hover {
            background-color: #0056b3;
        }

        .all-chats-btn {
            background-color: #007bff;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .chat-form button {
            margin-bottom: 10px;
            padding: 10px;
            border: none; /* Remove border */
            border-radius: 5px;
            font-size: 16px;
            transition: background-color 0.3s ease;
            background-color: #007bff; /* Match background color with "All Chats" button */
            color: #fff; /* Text color for dark theme */
            cursor: pointer;
        }

        .chat-form button:hover {
            background-color: #0056b3; /* Darker color on hover */
        }
    </style>
</head>

<body>
    <div class="chat-container" id="chatContainer">
        <h3>Write new chat..</h3>
        <form class="chat-form" method="POST" action="/chats">
            <input placeholder="Who are you?" name="from" />
            <textarea name="msg" placeholder="Write your message here.."></textarea>
            <input placeholder="Add receiver" name="to" />
            <button type="submit">Create Chat</button>
        </form>
        <button class="all-chats-btn" onclick="location.href='/chats';" type="button">ALL CHATS</button>
    </div>
</body>

</html>
